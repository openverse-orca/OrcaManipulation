# 6 点路点  2026-07-18 16:53:41
# 坐标系：G1 OmniPicker base_body（body_link1）
# 底座右移补偿：delta_b=[0.0, -0.014278, 0.0]；pos'=pos-delta_b（y += 0.014278）
# 累计 reloc Y：-0.141338 -0.068396 +0.014278 = -0.195456（相对最初）
# 语义：接近 / 抓取 / 经由1 / 经由2 / 箱上方 / 松开

gripper_open: -0.8561
gripper_close: 2.0

segments:

  # 路点 1  接近（抓取）
  - steps: 300
    l_hold: true
    r_target_b: [0.5745, -0.5084, 0.1909]
    r_quat_b: [-0.4633, 0.4682, 0.5474, 0.5163]
    gripper_r: open

  # 路点 2  抓取闭爪
  - steps: 300
    l_hold: true
    r_target_b: [0.5770, -0.5232, 0.1848]
    r_quat_b: [-0.4587, 0.4771, 0.5340, 0.5261]
    gripper_r: close

  # 路点 3  放箱经由1
  - steps: 300
    l_hold: true
    r_target_b: [0.6788, -0.0182, 0.4808]
    r_quat_b: [-0.5427, -0.2961, 0.4458, 0.6473]
    gripper_r: close

  # 路点 4  放箱经由2
  - steps: 300
    l_hold: true
    r_target_b: [0.6690, 0.1026, 0.4191]
    r_quat_b: [-0.4673, -0.3541, 0.4946, 0.6415]
    gripper_r: close

  # 路点 5  工具箱上方 / 入箱前位
  - steps: 300
    l_hold: true
    r_target_b: [0.5867, 0.1902, 0.2037]
    r_quat_b: [-0.6557, -0.2457, 0.6428, 0.3107]
    gripper_r: close

  # 路点 6  箱上松开位
  - steps: 300
    l_hold: true
    r_target_b: [0.5864, 0.1904, 0.2046]
    r_quat_b: [-0.6526, -0.2458, 0.6444, 0.3138]
    gripper_r: open
