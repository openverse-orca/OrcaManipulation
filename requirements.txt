# 核心运行依赖
orca-gym>=26.6.0
numpy
scipy
# 以 orca-gym 为准锁定；覆盖 lerobot 声明的 gymnasium<1.0.0（本项目仅用 lerobot 数据集 API）
gymnasium==1.2.1
opencv-python
av
pyarrow
# 使用仓库自带 third_party/lerobot（含数采补丁）。不要 pip install lerobot==0.3.4（网上没有这个包）
-e ./third_party/lerobot
mujoco
numba
termcolor>=2.4.0
pyyaml
h5py

# 可选：在线推理需要 openpi_client
# 安装方式一：pip install openpi_client
# 安装方式二：export OPENPI_CLIENT_SRC=<openpi-client源码路径>/src
